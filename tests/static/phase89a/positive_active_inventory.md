# Active Phase 89 inventory

The current virtual Irvine32 output set after `INCLUDE Irvine32.inc` implements direct `CALL Crlf`, zero-argument `INVOKE Crlf`, direct `CALL WriteChar`, and direct `CALL WriteString`.
