# Separate classifications

Remaining numeric output routines such as `WriteInt`, plus unimplemented Irvine32 `INVOKE` forms, remain deferred simulator work.

WinAPI execution, PE loading, host callbacks, native processes, and full x86 emulation are permanent product non-goals.
