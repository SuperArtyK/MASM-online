# Same-sentence exact-form distinction

Direct `CALL WriteString`, direct `CALL WriteDec`, and direct `CALL WriteInt` are implemented, while `INVOKE WriteString`, `INVOKE WriteDec`, and `INVOKE WriteInt` remain deferred.
