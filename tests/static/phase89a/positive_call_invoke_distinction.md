# Exact source-form distinction

Direct `CALL WriteString` is implemented and executable. Direct `CALL WriteDec` is implemented and executable. `INVOKE WriteString` and `INVOKE WriteDec` remain deferred until their owning future phases.
